{
  "name": "root",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "lerna": "^5.5.2"
  },
  "scripts": {
    "lint": "lerna run --stream lint",
    "prepare": "lerna bootstrap",
    "develop": "lerna run --parallel --stream develop",
    "solo": "lerna run start --scope=@borg-plays/solo-client"
  }
}
